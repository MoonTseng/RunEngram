# RunEngram 使用指南

## 它解决什么问题

RunEngram 让开发者和 AI Coding Agent 使用同一份研发任务事实：

- 开发者通过网页创建任务、补充上下文、调整优先级和查看验证结果；
- Codex 或其他 Agent 通过 CLI 领取任务、同步状态和提交交付证据；
- 网页、CLI 和 Agent Skill 共享同一个服务与 SQLite 数据库；
- 任务中断后，可以根据状态、文档和历史恢复，而不是重新解释全部背景。

当前 Alpha 版本重点验证任务执行闭环。可验证探索缓存和经验晋升属于下一
阶段。

## 1. 启动 RunEngram

首次构建：

```bash
git clone https://github.com/MoonTseng/RunEngram.git
cd RunEngram
./scripts/build.sh
cp .env.example .env
```

启动服务：

```bash
./dist/taskline-server
```

浏览器打开：

```text
http://127.0.0.1:8787/
```

本地后台启动也可以使用：

```bash
./scripts/start-local.sh
```

## 2. 创建项目

在终端设置服务地址，并创建项目：

```bash
export TASKLINE_SERVER=http://127.0.0.1:8787

./dist/taskline status
./dist/taskline register --name my-agent
./dist/taskline project create \
  --name my-project \
  --description "My engineering project"
```

`register` 只在 `status` 返回 `registered=false` 时执行。Agent 身份按工作
目录保存，不会覆盖其他项目身份。

## 3. 创建一个可执行任务

网页右上角选择“新建任务”，至少写清：

- 任务标题：期望交付的结果；
- 背景和目标：为什么做、影响范围、已知约束；
- 验收条件：哪些可验证结果代表完成；
- 工作类型和优先级；
- 相关 Markdown、图片、链接或依赖任务。

CLI 示例：

```bash
export TASKLINE_PROJECT=my-project

./dist/taskline task create \
  --title "Move URL methods to the new service" \
  --type feature \
  --priority 1 \
  --label refactor
```

## 4. 让 Agent 领取并执行

安装本地 CLI 和 Agent Skill：

```bash
./scripts/install-local.sh
```

进入需要修改的代码仓库：

```bash
cd /path/to/your-project
export TASKLINE_SERVER=http://127.0.0.1:8787
export TASKLINE_PROJECT=my-project

taskline status
taskline task next
taskline task next --claim
```

`task next` 只预览；`task next --claim` 才会原子领取任务。

在 Codex 中可以描述：

```text
请按照 RunEngram taskline-management Skill 领取 my-project 的下一项可执行
任务。开始前复述目标和验收条件；先完成代码分析并同步方案；开发过程中更新
任务阶段；完成后附上变更摘要、验证命令和测试结果。证据不足时不要标记完成。
```

## 5. 理解七个阶段

| 阶段 | 含义 |
| --- | --- |
| `pending` | 想法或暂未进入执行队列的工作 |
| `start` | 已具备目标和基本验收条件，可以领取 |
| `spec` | 明确需求、范围、UX 和验收标准 |
| `dev` | 技术设计、任务拆分和代码实现 |
| `test` | 本地构建、单元测试、集成测试和回归 |
| `review` | PR、代码评审和 CI |
| `done` | 交付证据满足服务端门禁 |

任务可以退回前一阶段。评审发现缺陷时应回到 `dev`；需求不清时应回到
`spec`，不需要删除并重建任务。

## 6. 给任务补充上下文

任务支持：

- Markdown 描述；
- 标签；
- 图片；
- Markdown 文档；
- 外部链接；
- 前置依赖；
- 完整变更历史。

附加 Markdown 文档：

```bash
taskline task doc create <完整任务ID> \
  --title "Technical design" \
  --file ./technical-design.md
```

补充进展：

```bash
taskline task update <完整任务ID> \
  --add-label review \
  --append-description "Implementation complete; local tests passed."
```

更新、文档和依赖操作应使用完整任务 ID。

## 7. 查看任务和依赖

网页提供两种视图：

- 看板：按阶段查看任务，支持列内滚动、排序和拖动状态；
- 依赖图：查看任务阻塞关系和执行顺序。

CLI 常用命令：

```bash
taskline task list
taskline task list --runnable
taskline task history <完整任务ID>
taskline task next
```

## 8. 验证完成

进入 `review` 前需要附加有效 GitHub PR。进入 `done` 时，服务端检查：

- PR 已合并；
- Review 对话已解决；
- CI 为绿色，或仓库没有配置 CI 检查。

`--force` 只能绕过领取所有权，不能绕过交付证据。

## 9. 试用时观察什么

- 创建任务是否足够快；
- Agent 是否减少重复追问；
- 中断后是否能快速恢复；
- 是否减少重复代码搜索和错误路径；
- 方案、验证结果和关键决策是否自然沉淀；
- 返工次数和人工干预是否下降；
- 哪些经验值得晋升为项目知识、Skill、测试或门禁。

这些指标决定 RunEngram 是否真正提升研发效率，而不是只把 Agent 状态画成
看板。
